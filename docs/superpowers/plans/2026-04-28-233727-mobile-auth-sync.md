# 移动端登录注册真实接口对接 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task. It will decide whether each batch should run in parallel or serial subagent mode and will pass only task-local context to each subagent. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让移动端 `login/register` 与桌面端一致接入真实认证接口，并实现按角色预填测试账号与正确鉴权跳转。  
**Architecture:** 复用现有 `authApi`、`resolveLoginRoleDecision`、`userAuthStore/enterpriseAuthStore`，仅改造移动端页面行为层；通过新增页面级测试先失败后实现，保证核心认证路径可回归。  
**Tech Stack:** Next.js + React + TypeScript + Vitest + Testing Library

---

### Task 1: 为移动登录页补齐行为测试（AC-001/002/003/004）

**Files:**
- Create: `frontend/tests/pages/mobile-user-login.test.tsx`
- Test: `frontend/tests/pages/mobile-user-login.test.tsx`

- [ ] **Step 1: 写失败测试，覆盖默认测试账号与角色切换预填**
- [ ] **Step 2: 写失败测试，覆盖调用 `authApi.login` + store 写入 + 跳转**
- [ ] **Step 3: 写失败测试，覆盖接口异常/角色不匹配报错不跳转**
- [ ] **Step 4: 运行 `npm run test:run -- tests/pages/mobile-user-login.test.tsx`，确认失败**

### Task 2: 实现移动登录页真实认证逻辑（AC-001/002/003/004）

**Files:**
- Modify: `frontend/src/app/mobile-user/login/page.tsx`
- Test: `frontend/tests/pages/mobile-user-login.test.tsx`

- [ ] **Step 1: 引入 `authApi`、`resolveLoginRoleDecision`、`userAuthStore`、`enterpriseAuthStore`**
- [ ] **Step 2: 角色值改为 `jobseeker/recruiter` 并实现角色切换自动预填测试账号**
- [ ] **Step 3: 实现 `handleSubmit` 调用登录接口、角色校验、store 写入与路由跳转**
- [ ] **Step 4: 实现错误态与 loading 态**
- [ ] **Step 5: 运行 `npm run test:run -- tests/pages/mobile-user-login.test.tsx`，确认通过**

### Task 3: 为移动注册页补齐行为测试（AC-005/006/007/008）

**Files:**
- Create: `frontend/tests/pages/mobile-user-register.test.tsx`
- Test: `frontend/tests/pages/mobile-user-register.test.tsx`

- [ ] **Step 1: 写失败测试，覆盖验证码发送接口调用与失败提示**
- [ ] **Step 2: 写失败测试，覆盖招聘者注册走 `companyRegister`**
- [ ] **Step 3: 写失败测试，覆盖审核中提示后跳转 `/login?review=pending`**
- [ ] **Step 4: 运行 `npm run test:run -- tests/pages/mobile-user-register.test.tsx`，确认失败**

### Task 4: 实现移动注册页真实接口与校验逻辑（AC-005/006/007/008）

**Files:**
- Modify: `frontend/src/app/mobile-user/register/page.tsx`
- Test: `frontend/tests/pages/mobile-user-register.test.tsx`

- [ ] **Step 1: 引入 `authApi`、stores、注册请求类型**
- [ ] **Step 2: 实现邮箱验证码发送、倒计时、错误映射**
- [ ] **Step 3: 实现求职者/招聘者差异化注册提交**
- [ ] **Step 4: 实现成功写入 store 与跳转；审核中分支提示与延时跳转**
- [ ] **Step 5: 运行 `npm run test:run -- tests/pages/mobile-user-register.test.tsx`，确认通过**

### Task 5: 全量验证与浏览器验收

**Files:**
- Modify: `frontend/tests/pages/mobile-user-login.test.tsx`（若需调整）
- Modify: `frontend/tests/pages/mobile-user-register.test.tsx`（若需调整）

- [ ] **Step 1: 运行 `npm run build`**
- [ ] **Step 2: 运行 `npm run test:run`**
- [ ] **Step 3: 使用 CDP 打开 `http://localhost:8888/login` 进行移动端登录/注册流程验收**
- [ ] **Step 4: 记录验证结果并准备提交**
