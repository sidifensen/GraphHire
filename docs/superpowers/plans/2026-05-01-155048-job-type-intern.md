# Job Type 扩展（全职/兼职/实习）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task. It will decide whether each batch should run in parallel or serial subagent mode and will pass only task-local context to each subagent. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将后端 `job_type` 统一为 `1=全职,2=兼职,3=实习`，并同步数据库约束、注释、迁移脚本与代码注释。  
**Architecture:** 保持 `SMALLINT` 存储与现有 DTO 结构不变，通过更新 `schema.sql` 与新增 migration 保证新库/旧库都一致，并通过单元测试锁定约束与注释文本。  
**Tech Stack:** Java 21, Spring Boot, JUnit 5, PostgreSQL SQL scripts, Maven

---

### Task 1: 先写失败测试（TDD Red）

**Files:**
- Create: `backend/src/test/java/com/graphhire/job/infrastructure/persistence/JobSchemaSqlTest.java`
- Test: `backend/src/test/java/com/graphhire/job/infrastructure/persistence/JobSchemaSqlTest.java`

- [ ] **Step 1: Write the failing test**

```java
// 断言 schema.sql 必须包含 IN (1, 2, 3) 与三类型注释
```

- [ ] **Step 2: Run test to verify it fails**

Run: `mvn -Dtest=JobSchemaSqlTest test`  
Expected: FAIL（当前 schema 仅允许 1/2，注释缺少实习）

### Task 2: 修改生产 SQL 与注释（TDD Green）

**Files:**
- Modify: `backend/src/main/resources/db/schema.sql`
- Create: `backend/src/main/resources/db/migration/V2026_05_01_014__extend_job_type_to_intern.sql`
- Modify: `backend/src/main/java/com/graphhire/job/domain/model/Job.java`
- Modify: `backend/src/main/java/com/graphhire/job/infrastructure/persistence/po/JobPO.java`

- [ ] **Step 1: Update schema.sql**

```sql
CONSTRAINT chk_job_type CHECK (job_type IN (1, 2, 3))
COMMENT ON COLUMN job.job_type IS '工作类型：1-全职 2-兼职 3-实习';
```

- [ ] **Step 2: Add migration SQL for existing DB**

```sql
ALTER TABLE job DROP CONSTRAINT IF EXISTS chk_job_type;
ALTER TABLE job ADD CONSTRAINT chk_job_type CHECK (job_type IN (1, 2, 3));
COMMENT ON COLUMN job.job_type IS '工作类型：1-全职 2-兼职 3-实习';
```

- [ ] **Step 3: Sync Java comments**

```java
/** 工作类型（1=全职 2=兼职 3=实习） */
```

- [ ] **Step 4: Re-run test to verify it passes**

Run: `mvn -Dtest=JobSchemaSqlTest test`  
Expected: PASS

### Task 3: 全量验证与提交

**Files:**
- Modify: `docs/superpowers/specs/2026-05-01-155048-job-type-intern-design.md`
- Modify: `docs/superpowers/acceptance/2026-05-01-155048-job-type-intern-acceptance.md`
- Modify: `docs/superpowers/plans/2026-05-01-155048-job-type-intern.md`
- Modify: `RELEASE-NOTES.md`

- [ ] **Step 1: Backend compile and tests**

Run: `cd backend && mvn compile && mvn test`  
Expected: PASS

- [ ] **Step 2: Frontend build and tests**

Run: `cd frontend && npm run build && npm run test:run`  
Expected: PASS

- [ ] **Step 3: Browser validation via CDP**

Run: 使用 `chrome-devtools` 打开前端页面，确认职位相关页面可加载。  
Expected: 页面正常显示，无关键报错。

- [ ] **Step 4: Update release notes and commit**

Run:
```bash
git add <changed files>
git commit -m "fix: 扩展职位类型支持实习并同步数据库约束与注释"
```
Expected: commit success
