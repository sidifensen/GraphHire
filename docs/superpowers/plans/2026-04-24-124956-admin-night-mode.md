# Admin Night Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task. It will decide whether each batch should run in parallel or serial subagent mode and will pass only task-local context to each subagent. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修正管理端夜间模式三层背景（菜单黑、主区灰、卡片黑）

**Architecture:** 在壳层组件注入稳定暗色类名，在全局样式里做局部范围覆写，避免影响非管理端页面。

**Tech Stack:** Next.js, Tailwind CSS, Vitest, Testing Library

---

### Task 1: 先写失败测试

**Files:**
- Modify: `frontend/src/tests/pages/admin-shell.test.tsx`
- Test: `frontend/src/tests/pages/admin-shell.test.tsx`

- [x] **Step 1: 写失败测试**
- [x] **Step 2: 运行测试并确认失败**
- [x] **Step 3: 记录失败原因（缺少目标暗色类/测试标识）**

### Task 2: 最小实现

**Files:**
- Modify: `frontend/src/components/admin/AdminSidebar.tsx`
- Modify: `frontend/src/components/admin/AdminShell.tsx`
- Modify: `frontend/src/styles/globals.css`

- [x] **Step 1: 侧栏增加暗色黑底与测试标识**
- [x] **Step 2: 主内容区增加暗色灰底与语义类**
- [x] **Step 3: 主内容区内卡片黑底局部覆写**

### Task 3: 验证

**Files:**
- Test: `frontend/src/tests/pages/admin-shell.test.tsx`

- [x] **Step 1: 重新运行目标测试并通过**
- [ ] **Step 2: 执行前后端编译/测试全量验证**
- [ ] **Step 3: 浏览器暗色模式人工验收**
