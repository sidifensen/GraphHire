# 企业端桌面菜单切换动效验收标准

## 功能验收

1. 企业端桌面顶部菜单存在统一的导航轨道视觉层。
2. 当前激活菜单存在可见滑块高亮，且在菜单切换时平滑过渡。
3. 菜单项在 hover/active 时具有轻微过渡反馈（颜色/位移/缩放之一或组合）。
4. 移动端头部与底部导航样式和交互不受影响。

## 质量验收

1. 组件测试覆盖桌面导航轨道与激活态指示器节点存在性。
2. 前端构建通过：`npm run build`。
3. 前端测试通过：`npm run test:run`。
4. 浏览器实测通过：在企业端桌面页面切换菜单可稳定触发过渡动画。
