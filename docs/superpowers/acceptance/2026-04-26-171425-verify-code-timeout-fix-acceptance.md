# 验证码超时与中文错误提示验收标准

1. 当验证码接口发生请求超时时，前端提示为 `请求超时，请稍后重试`，不得出现 `timeout of 30000ms exceeded`。
2. 当后端邮件发送失败时，接口返回中文业务错误：`验证码发送失败，请检查邮箱地址或稍后重试`。
3. `sendVerifyCode` 前端请求应使用 60000ms 超时配置。
4. 后端 SMTP 配置包含连接、读取、写入超时，避免长时间阻塞。
5. 对应前后端单元测试通过。
