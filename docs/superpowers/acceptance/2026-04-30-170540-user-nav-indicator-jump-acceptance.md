# 用户端顶部导航激活态漂移修复验收标准

## 功能验收
- [ ] 桌面端从首页滚动后切换到“职位/公司/我的”时，激活态背景只在 X 轴平滑移动，不出现从页面下方漂移的路径。
- [ ] 在 `/`、`/jobs`、`/companies`、`/profile` 间互相切换，激活态背景连续可见，位置与当前菜单项对齐。
- [ ] 浏览器窗口尺寸变化后，激活态背景仍与当前菜单项宽度和位置对齐。

## 自动化验收
- [ ] `frontend/src/tests/components/mock-user-navbar-indicator-metrics.test.ts` 通过。

## 回归验收
- [ ] 现有导航登录态显示（用户名/头像）不回归。
