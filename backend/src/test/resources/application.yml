management:
  health:
    mail:
      enabled: false

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/graphhire
    username: postgres
    password: postgres
  data:
    redis:
      host: localhost
      port: 6379
      database: 2
